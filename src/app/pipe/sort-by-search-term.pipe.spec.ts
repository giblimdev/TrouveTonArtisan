import { SortBySearchTermPipe } from './sort-by-search-term.pipe';

describe('SortBySearchTermPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBySearchTermPipe();
    expect(pipe).toBeTruthy();
  });
});
