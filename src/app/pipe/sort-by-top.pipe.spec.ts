import { SortByTopPipe } from './sort-by-top.pipe';

describe('SortByTopPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByTopPipe();
    expect(pipe).toBeTruthy();
  });
});
